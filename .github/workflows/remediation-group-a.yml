name: Remediation Group A

on:
  push:
    branches:
      - '1.0.12'

permissions:
  contents: write

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Probe write-capable runner
        run: echo remediation-write-runner-ok
