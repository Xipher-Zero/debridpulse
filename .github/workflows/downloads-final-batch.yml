name: Final Downloads Batch Helper

on:
  push:
    branches:
      - ci-ops/downloads-final-f68b0a8

permissions:
  contents: write

env:
  TARGET_BRANCH: staging/v1.0.11-ui
  EXPECTED_BASE: f68b0a8a7a4662ab29599e48d6efffc38ba37bd2
  HELPER_BRANCH: ci-ops/downloads-final-f68b0a8

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout helper branch
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          fetch-depth: 0

      - name: Set up Python 3.12
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: |
            backend/requirements.txt
            backend/requirements-dev.txt

      - name: Install test dependencies
        run: |
          cd backend
          python -m pip install -r requirements-dev.txt

      - name: Apply reviewed batch fail-closed
        shell: bash
        run: |
          set -euo pipefail

          git show "origin/$HELPER_BRANCH:.github/scripts/apply_downloads_final_batch.py" > /tmp/apply_downloads_final_batch.py
          git show "origin/$HELPER_BRANCH:.github/scripts/prepare_downloads_final_helper.py" > /tmp/prepare_downloads_final_helper.py
          python -m py_compile /tmp/apply_downloads_final_batch.py /tmp/prepare_downloads_final_helper.py
          python /tmp/prepare_downloads_final_helper.py /tmp/apply_downloads_final_batch.py
          python -m py_compile /tmp/apply_downloads_final_batch.py

          git fetch origin "$TARGET_BRANCH"
          actual_base="$(git rev-parse "origin/$TARGET_BRANCH")"
          if [ "$actual_base" != "$EXPECTED_BASE" ]; then
            echo "Target branch moved: expected $EXPECTED_BASE, got $actual_base"
            exit 1
          fi

          git checkout --detach "$EXPECTED_BASE"
          python /tmp/apply_downloads_final_batch.py

          test "$(tr -d '\r\n' < VERSION)" = "1.0.10"
          git diff --check
          node --check frontend/static/ui-downloads-runtime.js
          node --check frontend/static/ui-runtime.js
          node --check frontend/static/operator-title.js

          while IFS= read -r changed; do
            case "$changed" in
              frontend/static/index.html|\
              frontend/static/operator-title.js|\
              frontend/static/ui-runtime.js|\
              frontend/static/ui-downloads-runtime.js|\
              frontend/static/ui-dashboard-control-polish.css|\
              frontend/static/ui-downloads-page.css|\
              frontend/static/ui-feature-icon-contract.css|\
              frontend/static/ui-transfer-contract.css|\
              frontend/static/style-v11.css|\
              backend/tests/test_*.py)
                ;;
              *)
                echo "Unexpected changed path: $changed"
                exit 1
                ;;
            esac
          done < <(git diff --name-only)

          (cd backend && python -m pytest tests/ -q)

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add frontend/static/index.html \
                  frontend/static/operator-title.js \
                  frontend/static/ui-runtime.js \
                  frontend/static/ui-downloads-runtime.js \
                  frontend/static/ui-dashboard-control-polish.css \
                  frontend/static/ui-downloads-page.css \
                  frontend/static/ui-feature-icon-contract.css \
                  frontend/static/ui-transfer-contract.css \
                  frontend/static/style-v11.css \
                  backend/tests
          git diff --cached --check
          git commit -m "Polish Downloads selection and pagination controls"

          git fetch origin "$TARGET_BRANCH"
          current="$(git rev-parse "origin/$TARGET_BRANCH")"
          if [ "$current" != "$EXPECTED_BASE" ]; then
            echo "Target branch moved before push: expected $EXPECTED_BASE, got $current"
            exit 1
          fi

          git push origin HEAD:"refs/heads/$TARGET_BRANCH"
