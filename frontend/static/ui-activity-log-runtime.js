/* Activity Log server-filtering and control presentation owner. */
(function(){
'use strict';
const EVENT_LIMIT=500,SEARCH_DEBOUNCE_MS=250,TIMEFRAMES=Object.freeze([['1h','Last hour'],['12h','Last 12 hours'],['24h','Last day'],['72h','Last 3 days'],['7d','Last week'],['30d','Last 30 days'],['all','Available history']]),SEVERITIES=Object.freeze([['','All'],['info','Info'],['warning','Warning'],['error','Error']]);let generation=0,timer=null;
function filters(c){return Boolean(c.search?.value?.trim()||(c.timeframe?.value||'all')!=='all'||(c.severity?.value||'')!=='');}
function syncReset(c){if(c?.reset)c.reset.hidden=!filters(c);}
function setOptions(select,defs,fallback){if(!select)return;const current=select.value;select.replaceChildren(...defs.map(([value,label])=>{const o=document.createElement('option');o.value=value;o.textContent=label;return o;}));select.value=defs.some(([v])=>v===current)?current:fallback;}
function ensureField(select,kind){let field=select.closest('.dp-activity-filter-field');if(!field){field=document.createElement('label');field.className='dp-activity-filter-field';select.before(field);field.appendChild(select);}field.classList.toggle('dp-activity-filter-field--time',kind==='time');field.classList.toggle('dp-activity-filter-field--severity',kind==='severity');let label=field.querySelector(':scope > .dp-activity-filter-label');if(!label){label=document.createElement('span');label.className='dp-activity-filter-label';field.prepend(label);}if(kind==='time'){label.innerHTML='<span>Time</span><span>Window</span>';}else label.textContent='Severity';return field;}
function ensureControls(){const row=document.querySelector('#view-events .dp-activity-search-row'),search=document.getElementById('ev-search'),severity=document.getElementById('ev-level');if(!row||!search||!severity)return null;search.removeAttribute('oninput');severity.removeAttribute('onchange');let timeframe=document.getElementById('ev-timeframe');if(!timeframe){timeframe=document.createElement('select');timeframe.id='ev-timeframe';timeframe.className='input';row.appendChild(timeframe);}setOptions(timeframe,TIMEFRAMES,'all');setOptions(severity,SEVERITIES,'');if(window.DPDropdowns?.refresh)window.DPDropdowns.refresh();const timeField=ensureField(timeframe,'time'),severityField=ensureField(severity,'severity');let reset=document.getElementById('ev-reset');if(!reset){reset=document.createElement('button');reset.type='button';reset.id='ev-reset';reset.className='btn btn-ghost btn-sm';reset.textContent='Reset Filters';reset.hidden=true;}row.append(search,timeField,severityField,reset);const card=document.querySelector('#view-events > .dp-activity-card');let note=document.getElementById('dp-activity-result-note');if(!note&&card){note=document.createElement('div');note.id='dp-activity-result-note';note.className='dp-activity-result-note';note.hidden=true;card.insertBefore(note,document.getElementById('event-list')||null);}const c={search,timeframe,severity,reset,note};if(search.dataset.dpServerFilter!=='1'){search.dataset.dpServerFilter='1';search.addEventListener('input',()=>{syncReset(c);if(timer)clearTimeout(timer);timer=setTimeout(()=>{timer=null;void loadActivityEvents();},SEARCH_DEBOUNCE_MS);});}if(timeframe.dataset.dpServerFilter!=='1'){timeframe.dataset.dpServerFilter='1';timeframe.addEventListener('change',()=>{syncReset(c);void loadActivityEvents();});}if(severity.dataset.dpServerFilter!=='1'){severity.dataset.dpServerFilter='1';severity.addEventListener('change',()=>{syncReset(c);void loadActivityEvents();});}if(reset.dataset.dpServerFilter!=='1'){reset.dataset.dpServerFilter='1';reset.addEventListener('click',()=>{if(timer)clearTimeout(timer);search.value='';timeframe.value='all';severity.value='';syncReset(c);void loadActivityEvents();});}syncReset(c);return c;}
function parseTimestamp(value){if(!value)return null;try{if(typeof parseApiDate==='function')return parseApiDate(value);}catch(_){}const raw=String(value),d=new Date(/[zZ]|[+-]\d\d:\d\d$/.test(raw)?raw:`${raw.replace(' ','T')}Z`);return Number.isNaN(d.getTime())?null:d;}
function formatTimestamp(value){const d=parseTimestamp(value);if(!d)return String(value||'—');let zone='UTC';try{zone=String(settingsData?.timezone||'').trim()||'UTC';}catch(_){}try{return new Intl.DateTimeFormat('en-US',{timeZone:zone,year:'numeric',month:'short',day:'numeric',hour:'2-digit',minute:'2-digit',second:'2-digit'}).format(d);}catch(_){return d.toLocaleString();}}
function render(items,c){const list=document.getElementById('event-list');if(!list)return;list.replaceChildren();if(!items.length){const e=document.createElement('div');e.className='empty';e.textContent=filters(c)?'No events match your filters.':'No events yet.';list.appendChild(e);return;}for(const event of items){const row=document.createElement('div');row.className='dp-activity-row';const level=document.createElement('div');const sev=String(event.level||'info').toLowerCase()==='warn'?'warning':String(event.level||'info').toLowerCase();level.className=`elevel dp-activity-level ${sev==='warning'?'warn':sev==='error'?'error':'info'}`;level.setAttribute('aria-label',sev==='warning'?'Warning':sev==='error'?'Error':'Info');const copy=document.createElement('div');copy.className='dp-activity-copy';const msg=document.createElement('div');msg.className='emsg dp-activity-message';msg.textContent=String(event.message??'');copy.appendChild(msg);if(event.torrent_name){const name=document.createElement('div');name.className='ename dp-activity-transfer';name.textContent=String(event.torrent_name);copy.appendChild(name);}const time=document.createElement('div');time.className='etime dp-activity-time';time.textContent=formatTimestamp(event.created_at);row.append(level,copy,time);list.appendChild(row);}document.dispatchEvent(new CustomEvent('debridpulse:activity-rendered'));}
async function loadActivityEvents(){const c=ensureControls();if(!c||typeof api!=='function')return null;const owned=++generation,filtered=filters(c),params=new URLSearchParams();params.set('limit',String(EVENT_LIMIT));if(filtered){params.set('include_meta','true');params.set('timeframe',c.timeframe.value||'all');if(c.severity.value)params.set('level',c.severity.value);if(c.search.value.trim())params.set('search',c.search.value.trim());}try{const payload=await api('GET',`/events?${params.toString()}`);if(owned!==generation)return null;const items=Array.isArray(payload?.items)?payload.items:Array.isArray(payload)?payload:[];render(items,c);if(c.note){if(payload?.truncated===true){c.note.textContent=`Showing the latest ${EVENT_LIMIT} matching events. Narrow your filters to see older matches.`;c.note.hidden=false;}else{c.note.hidden=true;c.note.textContent='';}}syncReset(c);return payload;}catch(error){try{toast(sanitizeErrorMsg(error?.message||error),'error');}catch(_){}return null;}}
function bind(){ensureControls();try{loadEvents=loadActivityEvents;}catch(_){}try{filterEvents=loadActivityEvents;}catch(_){}window.loadEvents=loadActivityEvents;window.filterEvents=loadActivityEvents;}
window.DPActivityLog=Object.freeze({load:loadActivityEvents,formatTimestamp,timeframes:TIMEFRAMES.map(([v])=>v),severities:SEVERITIES.map(([v])=>v)});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();
})();