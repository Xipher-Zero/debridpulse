/* Automatic Extraction archive-password editor owner. */
(function(){
'use strict';
let rows=null,editorNode=null,sourceNode=null,keySerial=0,revealAll=false,activeKey=null,scheduled=false;
const source=()=>document.querySelector('#view-settings [data-panel="extraction"] [data-setting="extraction_password"]'),editor=()=>document.querySelector('#view-settings [data-panel="extraction"] .dp-settings-extraction-password-editor'),nextKey=()=>`row:${keySerial++}`;
function normalize(items){while(items.length>1&&items.at(-1).value===''&&items.at(-2).value==='')items.pop();if(!items.length||items.at(-1).value!=='')items.push({key:nextKey(),value:''});return items;}
function reset(s,e){const values=String(s?.value||'').replace(/\r\n?/g,'\n').split('\n').map(v=>v.trim()).filter(Boolean);rows=normalize(values.map(value=>({key:nextKey(),value})));sourceNode=s;editorNode=e;revealAll=false;activeKey=null;}
function syncSource(s){if(!s||!rows)return;s.value=rows.map(r=>String(r.value||'').trim()).filter(Boolean).join('\n');const clear=s.closest('[data-panel="extraction"]')?.querySelector('[data-clear-secret="extraction_password"]');if(clear)clear.checked=s.value.length===0;}
function eyeSvg(hidden){const ns='http://www.w3.org/2000/svg',svg=document.createElementNS(ns,'svg');svg.setAttribute('viewBox','0 0 24 24');svg.setAttribute('fill','none');svg.setAttribute('stroke','currentColor');svg.setAttribute('stroke-width','2');svg.setAttribute('aria-hidden','true');const add=d=>{const p=document.createElementNS(ns,'path');p.setAttribute('d',d);svg.appendChild(p);};if(hidden){add('m2 2 20 20');add('M6.7 6.7C4.6 8.1 3 10.3 3 12c0 0 3 7 9 7 1.9 0 3.5-.7 4.8-1.7');add('M9.9 4.2A8.4 8.4 0 0 1 12 4c6 0 9 7 9 8a10.2 10.2 0 0 1-1.7 2.7');}else{add('M2.1 12.3a1 1 0 0 1 0-.7C3.5 7.7 7.2 5 12 5s8.5 2.7 9.9 6.7a1 1 0 0 1 0 .7C20.5 16.3 16.8 19 12 19s-8.5-2.7-9.9-6.7');const c=document.createElementNS(ns,'circle');c.setAttribute('cx','12');c.setAttribute('cy','12');c.setAttribute('r','3');svg.appendChild(c);}return svg;}
function setEye(button){if(!button)return;const action=revealAll?'Hide all passwords':'Show all passwords',label=document.createElement('span');label.className='dp-settings-password-eye-label';label.textContent=revealAll?'Hide all':'Show all';button.classList.add('dp-settings-password-eye--ghost');button.setAttribute('aria-pressed',revealAll?'true':'false');button.setAttribute('aria-label',action);button.title=action;button.classList.toggle('is-open',revealAll);button.replaceChildren(eyeSvg(revealAll),label);}
function focusKey(e,key){if(!e||!key)return;requestAnimationFrame(()=>{const input=Array.from(e.querySelectorAll('.dp-settings-password-line')).find(n=>n.dataset.passwordKey===key);if(input){input.focus();try{input.setSelectionRange(input.value.length,input.value.length);}catch(_){}}});}
function render(e,s,focus=null){if(!e||!s||!rows)return;normalize(rows);let host=e.querySelector('.dp-settings-password-rows');if(!host){host=document.createElement('div');host.className='dp-settings-password-rows';e.prepend(host);}host.replaceChildren();rows.forEach((row,index)=>{const input=document.createElement('input');input.className='dp-settings-password-line';input.type=revealAll||activeKey===row.key?'text':'password';input.value=row.value;input.dataset.passwordKey=row.key;input.dataset.passwordIndex=String(index);input.autocomplete='off';input.spellcheck=false;input.setAttribute('aria-label',`Archive password ${index+1}`);if(index===rows.length-1&&!row.value)input.placeholder='Add an archive password';input.addEventListener('focus',()=>{input.dataset.passwordEditStart=row.value;activeKey=row.key;input.type='text';});input.addEventListener('input',()=>{row.value=input.value;syncSource(s);if(index===rows.length-1&&row.value!==''){normalize(rows);activeKey=row.key;render(e,s,row.key);}});input.addEventListener('blur',()=>{row.value=input.value;syncSource(s);queueMicrotask(()=>{const i=rows.indexOf(row);if(i>=0&&i<rows.length-1&&String(row.value||'').trim()===''){rows.splice(i,1);normalize(rows);activeKey=null;render(e,s);return;}if(document.activeElement?.closest('.dp-settings-extraction-password-editor')!==e&&!revealAll){activeKey=null;e.querySelectorAll('.dp-settings-password-line').forEach(n=>n.type='password');}});});input.addEventListener('keydown',event=>{if(event.key==='Escape'){event.preventDefault();row.value=input.dataset.passwordEditStart??row.value;input.value=row.value;syncSource(s);input.blur();return;}if(event.key==='Enter'){event.preventDefault();row.value=input.value;syncSource(s);const inserted={key:nextKey(),value:''};rows.splice(index+1,0,inserted);normalize(rows);activeKey=inserted.key;render(e,s,inserted.key);return;}if(event.key==='Backspace'&&input.value===''&&rows.length>1){event.preventDefault();rows.splice(index,1);normalize(rows);const target=rows[Math.max(0,index-1)];activeKey=target?.key||null;syncSource(s);render(e,s,target?.key||null);return;}if(event.altKey&&(event.key==='ArrowUp'||event.key==='ArrowDown')){const to=event.key==='ArrowUp'?index-1:index+1;if(to<0||to>=rows.length)return;event.preventDefault();[rows[index],rows[to]]=[rows[to],rows[index]];activeKey=row.key;syncSource(s);render(e,s,row.key);}});input.addEventListener('paste',event=>{const pasted=event.clipboardData?.getData('text')||'';if(!/[\r\n]/.test(pasted))return;event.preventDefault();const incoming=pasted.replace(/\r\n?/g,'\n').split('\n').map(value=>({key:nextKey(),value}));rows.splice(index,1,...incoming);normalize(rows);const target=incoming.at(-1);activeKey=target.key;syncSource(s);render(e,s,target.key);});host.appendChild(input);});let eye=e.querySelector('.dp-settings-password-eye');if(eye&&eye.dataset.dpArchiveOwner!=='1'){const replacement=eye.cloneNode(false);replacement.type='button';replacement.className=eye.className;replacement.dataset.dpArchiveOwner='1';eye.replaceWith(replacement);eye=replacement;eye.addEventListener('click',()=>{revealAll=!revealAll;if(!revealAll)activeKey=null;setEye(eye);e.querySelectorAll('.dp-settings-password-line').forEach(n=>n.type=revealAll?'text':'password');});}setEye(eye);e.dataset.dpArchiveOwner='1';syncSource(s);if(focus)focusKey(e,focus);}
function apply(){const s=source(),e=editor();if(!s||!e)return;if(e!==editorNode||s!==sourceNode||!rows)reset(s,e);const hint=s.closest('.dp-settings-extraction-password-field')?.querySelector(':scope > .form-hint'),copy='Add one password per line. Select a row to edit it; use the eye to show or hide all passwords.';if(hint&&hint.textContent!==copy)hint.textContent=copy;if(e.dataset.dpArchiveOwner!=='1')render(e,s);}
function scheduleApply(){if(scheduled)return;scheduled=true;queueMicrotask(()=>{scheduled=false;apply();});}
function init(){const settings=document.getElementById('view-settings');if(settings)new MutationObserver(scheduleApply).observe(settings,{childList:true,subtree:true});document.addEventListener('debridpulse:settings-rendered',scheduleApply);scheduleApply();}
window.DPArchivePasswords=Object.freeze({apply});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();